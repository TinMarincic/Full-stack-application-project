{
  "version": 2,
  "buildCommand": "pip install -r api/requirements.txt && pip install prisma && prisma generate --schema=prisma/schema.prisma",
  "devCommand": "pip install -r api/requirements.txt && pip install prisma && prisma generate --schema=prisma/schema.prisma",
  "routes": [
    {
      "src": "/(.*)",
      "dest": "api/main.py"
    }
  ]
}
