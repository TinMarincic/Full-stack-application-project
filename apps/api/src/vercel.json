{
  "version": 2,
  "buildCommand": "pip install prisma && prisma generate",
  "devCommand": "pip install prisma && prisma generate",
  "builds": [
    {
      "src": "main.py",
      "use": "@vercel/python"
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "main.py"
    }
  ]
}
