{
  "version": 2,
  "buildCommand": "pip install -r requirements.txt && pip install prisma && prisma generate --schema=prisma/schema.prisma",
  "devCommand": "pip install -r requirements.txt && pip install prisma && prisma generate --schema=prisma/schema.prisma",
  "builds": [
    {
      "src": "api/index.py",
      "use": "@vercel/python"
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "api/index.py"
    }
  ]
}
