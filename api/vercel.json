{
  "version": 2,
  "buildCommand": "cd api && pip install -r requirements.txt",
  "devCommand": "cd api && pip install -r requirements.txt",
  "builds": [
    { "src": "api/index.py", "use": "@vercel/python" }
  ],
  "routes": [
    { "src": "/(.*)", "dest": "api/index.py" }
  ]
}
