{
  "version": 2,
  "buildCommand": "cd api && pip install -r requirements.txt && npx prisma generate --schema=prisma/schema.prisma",
  "devCommand": "cd api && pip install -r requirements.txt && npx prisma generate --schema=prisma/schema.prisma",
  "builds": [
    { "src": "api/index.py", "use": "@vercel/python" }
  ],
  "routes": [
    { "src": "/(.*)", "dest": "api/index.py" }
  ]
}

