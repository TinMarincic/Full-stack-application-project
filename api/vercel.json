{
  "version": 2,
  "buildCommand": "npm install && npm run build && pip install -r requirements.txt",
  "devCommand": "pip install -r requirements.txt",
  "builds": [
    { "src": "index.py", "use": "@vercel/python" }
  ],
  "routes": [
    { "src": "/(.*)", "dest": "index.py" }
  ]
}
