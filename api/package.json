{
  "name": "api",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "npx prisma generate --schema=prisma/schema.prisma && uvicorn index:app --reload",
    "build": ""
  },
  "devDependencies": {
    "prisma": "^5.17.0"
  }
}
