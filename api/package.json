{
  "name": "api",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "npx prisma generate --schema=prisma/schema.prisma && uvicorn index:app --reload",
    "build": "npx prisma generate --schema=prisma/schema.prisma"
  },
  "devDependencies": {
    "prisma": "^5.17.0"
  }
}
